import { defineConfig } from 'prisma/config';

export default defineConfig({
  datasource: {
    url: process.env.DATABASE_URL || 'postgresql://bee_user:bee_pass_2026@localhost:5432/bee_recruit',
  },
});
